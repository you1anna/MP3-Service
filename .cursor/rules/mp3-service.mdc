---
description: MP3 service workspace rules
alwaysApply: true
---

# MP3 Service

Audio file processor for DJ workflow. Watches Soulseek downloads, processes to Pioneer XDJ-compatible formats.

Read `CLAUDE.md` first for architecture, paths, and testing commands.

## Key facts

- Canonical path: `/Users/macbookair/Dev/MP3-Service/python_service`
- Stale duplicate at `~/MP3-Service` — ignore it
- Venv: `python_service/.venv` (Python 3.12)
- Config: `python_service/config.json` (local only, not in git)
- launchd service: `com.macmini.mp3service`
- FLAC→AIFF conversion: 16-bit/44.1kHz
- `keep_flac_sources` (default `true`) decides whether source FLACs are deleted after conversion —
  deliberately independent of `ssd_archive_path`, since an unplugged drive must not change whether
  lossless originals are destroyed
- Runs on both the Mac mini and the MacBook Air. `config.json` is gitignored, so each carries its
  own and `git pull` does not carry new keys across — run `main.py doctor` on a machine to see how
  it resolves its config

## Testing

```sh
python3 main.py doctor              # machine-resolved config report
python3 main.py process --dry-run   # safe test
.venv/bin/python -m unittest discover -s tests   # full suite (unittest, not pytest)
```

## Service management

```sh
launchctl list | grep mp3service
launchctl bootout gui/$(id -u) ~/Library/LaunchAgents/com.macmini.mp3service.plist
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.macmini.mp3service.plist
```
